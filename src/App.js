// react hook = special function that allows functional components to use react reatures without writing class components (usestate, useEffect, useContext, useReducer, useCallback, ...)

// useState() = react hook that allows the creation of a stateful variable and a setter function to update its value in the virtual DOM
  //[name, setName]

  import MyComponent from "./MyComponent";

function App() {
  return (
    <>
      <MyComponent />
    </>
  );
}

export default App;
